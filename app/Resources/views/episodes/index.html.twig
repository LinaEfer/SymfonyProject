{% extends 'base.html.twig' %}

    {% block body %}
    <div class="container">
        <h1 class="center">Create a new episode</h1>
        {{ form_start(form) }}
             <div class="panel">
                 {{ form_widget(form) }}
                 <button type="submit" class="btn btn-primary"><a href="{{ path('homepage') }}">Back </a></button>
             </div>
        {{ form_end(form) }}
    </div>
{% endblock %}

